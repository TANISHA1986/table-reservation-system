<nav class="navbar">
    <div class="container">
        <div class="nav-brand">
            <% if (user) { %>
                <% if (user.role==='ADMIN' ) { %>
                    <a href="/admin/dashboard">◆ LUXURY DINING ◆</a>
                    <% } else { %>
                        <a href="/dashboard">◆ LUXURY DINING ◆</a>
                        <% } %>
                            <% } else { %>
                                <a href="/">◆ LUXURY DINING ◆</a>
                                <% } %>
        </div>
        <div class="nav-links">
            <% if (user) { %>
                <% if (user.role==='ADMIN' ) { %>
                    <a href="/admin/dashboard">Admin Dashboard</a>
                    <a href="/admin/tables">Manage Tables</a>
                    <a href="/admin/reservations">All Reservations</a>
                    <a href="/admin/checkin">Check-In Guests</a>
                    <a href="/admin/analytics">📊 Analytics</a>
                    <% } else { %>
                        <a href="/dashboard">Dashboard</a>
                        <a href="/book-table">Book Table</a>
                        <a href="/my-reservations">My Reservations</a>
                        <% } %>
                            <span class="user-info">👤 <%= user.name %></span>
                            <a href="#" onclick="logout(); return false;" class="btn-logout">Logout</a>
                            <% } else { %>
                                <a href="/login">Login</a>
                                <a href="/register">Register</a>
                                <% } %>
        </div>
    </div>
</nav>